package cs5004.animator.model;

/**
 * An enum class representing the different kinds of output available to generate IAnimations for.
 * New output types must be added here.
 */
public enum OutputType {
  TEXT, SVG, VISUAL
}
